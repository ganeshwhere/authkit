# How It Works

AuthKit is API-first:

- The Fastify auth service exposes versioned REST endpoints.
- SDKs wrap those endpoints with typed clients.
- Adapters isolate database, cache, and email providers.
- JWT signing uses RS256 and JWKS rotation support.
