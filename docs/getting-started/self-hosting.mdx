# Self Hosting

Use the Compose stack for local and production-like environments.

```bash
docker compose -f docker/docker-compose.yml up -d
```

Set required environment variables in `.env` before boot.
