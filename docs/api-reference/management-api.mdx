# Management API

Management endpoints live under `/v1/api`.

Required headers:

- `x-authkit-secret-key`
- `x-authkit-project-id`

Use these routes for server-side user administration, session revocation, audit queries, and webhook endpoint management.
