Reusable dashboard UI components live here.
Dashboard UI components live here, including:

- `dashboard-shell.tsx` for navigation and layout chrome
- `page-frame.tsx` for consistent page header framing
